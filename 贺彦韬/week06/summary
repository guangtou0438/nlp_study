# LLM 分类方法对比

## 实验设置

- 模型：Qwen2-0.5B-Instruct
- 评估：200条，val集，seed=42
- 训练数据：5000条（SFT两种方式相同）

## 结果

| 方法 | 准确率 | 无法解析 |
|------|--------|----------|
| Zero-shot | 0.36 | 58条 |
| LoRA SFT | 0.57 | 2条 |
| 全量SFT | 0.55 | 0条 |

## 备注

全量SFT过拟合严重，第2、3轮 val_loss 持续上升。

LoRA 只训练 0.07% 参数，效果反而略优于全量SFT。

Zero-shot 无法解析率高（29%），输出了大量不在类别列表中的词。
# BERT 文本分类实验报告

## 配置

- 模型：`bert-base-chinese` | Epochs：3 | Batch size：32 | LR：2e-5
- 数据：训练 53,360 / 验证 10,000 / 测试 10,000 | 类别数：15

## 池化策略对比

| 池化 | val_acc | macro F1 |
|------|---------|----------|
| CLS  | **0.5657** | 0.5515 |
| mean | 0.5641  | 0.5557   |
| max  | 0.5644  | **0.5613** |

三者差距极小，CLS 准确率略高，max 在少数类上更均衡。

## 类别 F1（CLS 池化）

表现好：体育 0.72、汽车 0.67、电竞 0.66（领域词汇鲜明）

表现差：证券 0.33（仅 45 条样本，严重不均衡）

整体：accuracy 0.57，macro F1 0.55

## 改进方向

1. `--use_class_weight` 处理证券等少数类
2. 增加 epochs（epoch 3 时模型仍未收敛）
3. 增大 `max_length`（当前 64）
